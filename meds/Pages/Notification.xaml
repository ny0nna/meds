<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="meds.Pages.Notification"
             Title="Уведомления">

    <StackLayout 
        x:Name="stackLayout"
             BackgroundColor="GhostWhite">
        <Label Text="10."
           TextColor="Red"
           HorizontalOptions="Center"
           VerticalOptions="Start" />
        <Button Text="Create Notification"
            HorizontalOptions="Center"
            VerticalOptions="Start"
            Clicked="OnSendClick" />

        <!--StackLayout Orientation="Horizontal" Padding="10,10,10,0">
            <Label Text="Интервал повторений " LineBreakMode="CharacterWrap" FontSize="16"  VerticalOptions="CenterAndExpand" TextColor="Black"/>
            <Entry x:Name="intervalTimer" HorizontalOptions="FillAndExpand" Keyboard="Numeric" WidthRequest="50" ClearButtonVisibility="WhileEditing"  TextColor="Black"/>
            <Label Text="ч." FontSize="16" VerticalOptions="CenterAndExpand" TextColor="Black"/>
        </StackLayout-->
        
        <Label Text="5." 
            TextColor="Red" 
           HorizontalOptions="Center"
           VerticalOptions="Start" />
         <Button Text="Create Notification"
           HorizontalOptions="Center"
            VerticalOptions="Start"
            Clicked="OnScheduleClick" />
    </StackLayout>
</ContentPage>