<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:meds"
       x:Class="meds.AppShell"
       FlyoutBackgroundColor="#b2c4d7"
       >

    <Shell.FlyoutHeader>
        <StackLayout BackgroundColor="#93a0ad" Padding="20" Orientation="Horizontal">
            <Image HeightRequest="50" Source="icon.ico"/>
            <Label Text="MEDS" FontSize="35" VerticalOptions="Center"/>
        </StackLayout>
    </Shell.FlyoutHeader>
    <FlyoutItem Title="Главная" Icon="xamarin.png">
        <ShellContent ContentTemplate="{DataTemplate pages:MainPage}"/>
    </FlyoutItem>
    <FlyoutItem Title="Page1" Icon="xamarin.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Page1}"/>
    </FlyoutItem>
        <FlyoutItem Title="Page2" Icon="xamarin.png">
            <ShellContent ContentTemplate="{DataTemplate pages:Page2}"/>
        </FlyoutItem>
    <MenuItem Clicked="MenuItem_Clicked" Text="Help"/>

</Shell>